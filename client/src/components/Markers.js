import { MarkerClusterer } from "@react-google-maps/api";
import React from "react";
import "./Marker.css";

const Marker = (coords) => {
  position = coords;
  return null;
};

export default Marker;
